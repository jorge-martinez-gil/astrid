# ASTRID Tabular Fault-Injection Experiment Dossier

Date: 2026-05-19  
Experiment: controlled tabular reliability fault injection  
Tool state: ASTRID headless tabular analyzer in `astrid_core.py`  
Primary output directory: `experiments/results/tabular_fault_injection_5seed/`

## Purpose

This dossier records the first repeated-seed empirical experiment for a future
top-tier ASTRID paper. The experiment evaluates whether ASTRID detects known,
controlled reliability failures in tabular datasets and whether its health
score, verdict, policy gate, and recommendations respond in the expected
direction as fault severity increases.

This supports the paper claim:

> A modality-aware pre-training dataset audit can surface measurable reliability
> risks before they propagate into model development workflows.

This run primarily addresses RQ1 from `docs/paper_blueprint.md`:

> How accurately does ASTRID detect injected dataset reliability failures across
> modalities?

It also gives early evidence for score monotonicity, recommendation coverage,
policy-gate behavior, and runtime.

## Reproduction Command

Run from the repository root:

```bash
python experiments/fault_injection_tabular.py --rows 4000 --seeds 7,11,19,23,31 --severities 0,0.01,0.05,0.10,0.20,0.40 --save-reports --out-dir experiments/results/tabular_fault_injection_5seed
```

In the local Codex runtime, the command was executed as:

```powershell
& "C:\Users\martinez\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe" experiments\fault_injection_tabular.py --rows 4000 --seeds 7,11,19,23,31 --severities 0,0.01,0.05,0.10,0.20,0.40 --save-reports
```

The local run was first written to the default result directory and then copied
into the clean curated directory above with only the current five-seed outputs.

## Generated Artifacts

The experiment generated:

- `experiments/results/tabular_fault_injection_5seed/tabular_fault_injection_summary.csv`
- `experiments/results/tabular_fault_injection_5seed/tabular_fault_injection_aggregate.csv`
- `experiments/results/tabular_fault_injection_5seed/tabular_fault_injection_results.json`
- `experiments/results/tabular_fault_injection_5seed/reports/*.json`
- `experiments/results/tabular_fault_injection_5seed/figures/score_vs_severity.svg`
- `experiments/results/tabular_fault_injection_5seed/figures/primary_metric_vs_severity.svg`
- `experiments/results/tabular_fault_injection_5seed/figures/detection_heatmap.svg`
- `experiments/results/tabular_fault_injection_5seed/figures/recommendation_heatmap.svg`
- `experiments/results/tabular_fault_injection_5seed/figures/policy_gate_heatmap.svg`

There are 180 per-run reports:

- 5 seeds: `7`, `11`, `19`, `23`, `31`.
- 6 fault types.
- 6 severities per fault type.
- 1 clean control at severity `0` per fault family and seed.

The generated `experiments/results/` directory is ignored by git, so selected
artifacts should be archived separately when preparing a paper artifact.

## Dataset Protocol

The clean dataset is generated by `make_clean_dataset()` in
`experiments/fault_injection_tabular.py`.

Dataset size:

- Requested clean rows per seed: 4,000.
- Rows increase for duplicate and split-leakage injections because those faults
  append corrupted copies.

Columns:

- `sample_id`: synthetic identifier.
- `timestamp`: deterministic temporal slices across four months.
- `split`: train, validation, or test.
- `site`: synthetic industrial plant group.
- `sensor_temp`, `sensor_pressure`, `vibration_rms`, `humidity`: numeric sensor
  features.
- `operator_note`: text field for PII injection.
- `target`: binary label.

ASTRID configuration:

- Analyzer: tabular.
- Preset: `Balanced (recommended)`.
- Mode: `Quick Scan`.
- Label column: `target`.
- Split column: `split`.
- Time column: `timestamp`.
- Group column: `site`.
- ID column: `sample_id`.

## Fault-Injection Protocol

Each fault is injected independently into the clean dataset. This isolates the
effect of one failure mode at a time.

| Fault Type | Injected Failure | Primary ASTRID Metric |
| --- | --- | --- |
| `missingness` | Random missing numeric cells. | `overall_missing_rate` |
| `duplicates` | Exact duplicate row insertion. | `exact_duplicate_row_rate` |
| `split_leakage` | Duplicated samples moved across train/test splits. | `row_hash_cross_split_rate` |
| `drift` | Numeric feature shifts in the final temporal slice. | `max_numeric_drift_ks` |
| `pii` | Synthetic email-like strings in `operator_note`. | `pii_flagged_columns` |
| `fairness` | Group-dependent positive-rate disparity. | `positive_rate_disparity` |

Severity grid:

| Severity Values |
| --- |
| `0`, `0.01`, `0.05`, `0.10`, `0.20`, `0.40` |

Detection thresholds used by the summary script:

| Fault Type | Detection Rule |
| --- | --- |
| `missingness` | `overall_missing_rate > 0.05` |
| `duplicates` | `exact_duplicate_row_rate > 0.01` |
| `split_leakage` | `row_hash_cross_split_rate > 0` |
| `drift` | `max_numeric_drift_ks > drift_ks_threshold` |
| `pii` | `pii_flagged_columns > 0` |
| `fairness` | `positive_rate_disparity > 0.20` |

## Runtime

Total runtime over 180 runs: 41.188 seconds  
Mean runtime per run: 0.229 seconds  
Maximum runtime per run: 0.538 seconds

This suggests the tabular analyzer is fast enough for CI-style dataset gates on
small-to-medium tabular datasets. Larger scaling experiments are still required
for rows, columns, and multimodal inputs.

## Health Score Mean by Fault Severity

| fault_type | 0 | 0.01 | 0.05 | 0.1 | 0.2 | 0.4 |
| --- | --- | --- | --- | --- | --- | --- |
| drift | 91.4 | 91.4 | 89.8 | 84.2 | 78.2 | 78.2 |
| duplicates | 91.4 | 90.4 | 85.8 | 80.4 | 79.2 | 79.4 |
| fairness | 91.4 | 91.6 | 89.8 | 87.6 | 83.4 | 81.8 |
| missingness | 91.4 | 91.2 | 90.4 | 89.2 | 87 | 83.2 |
| pii | 91.4 | 76.4 | 66.4 | 66.4 | 66.4 | 66.4 |
| split_leakage | 91.4 | 79.6 | 79.6 | 79.2 | 79 | 79.6 |

## Health Score Standard Deviation by Fault Severity

| fault_type | 0 | 0.01 | 0.05 | 0.1 | 0.2 | 0.4 |
| --- | --- | --- | --- | --- | --- | --- |
| drift | 1.817 | 1.517 | 1.643 | 2.049 | 0.447 | 0.447 |
| duplicates | 1.817 | 1.817 | 1.924 | 2.302 | 2.387 | 1.673 |
| fairness | 1.817 | 2.074 | 1.643 | 2.074 | 1.817 | 1.924 |
| missingness | 1.817 | 1.789 | 2.302 | 2.683 | 1.732 | 1.924 |
| pii | 1.817 | 13.446 | 1.817 | 1.817 | 1.817 | 1.817 |
| split_leakage | 1.817 | 1.673 | 2.302 | 1.789 | 2.121 | 1.517 |

Interpretation:

- Mean score decreases under every injected fault family.
- PII and split leakage trigger large early score penalties, reflecting their
  critical risk semantics.
- Missingness and drift degrade gradually until their active thresholds are
  crossed.
- PII at severity `0.01` has high score variance because some seeds cross the
  regex hit-rate threshold and others do not.

## Primary Metric Mean by Fault Severity

| fault_type | 0 | 0.01 | 0.05 | 0.1 | 0.2 | 0.4 |
| --- | --- | --- | --- | --- | --- | --- |
| drift | 0.102 | 0.099 | 0.126 | 0.208 | 0.374 | 0.646 |
| duplicates | 0 | 0.01 | 0.048 | 0.091 | 0.167 | 0.286 |
| fairness | 0.025 | 0.017 | 0.109 | 0.206 | 0.417 | 0.799 |
| missingness | 0 | 0.003 | 0.015 | 0.028 | 0.054 | 0.099 |
| pii | 0 | 0.6 | 1 | 1 | 1 | 1 |
| split_leakage | 0 | 0.01 | 0.05 | 0.1 | 0.2 | 0.4 |

Interpretation:

- The primary metrics move monotonically or near-monotonically with severity.
- Split leakage is exactly recovered because the injected failure is constructed
  with duplicated cross-split rows.
- PII is summarized as flagged-column count, which saturates early. A future
  paper table should also include PII hit rate for smoother severity analysis.

## Detection Rate Matrix

Values are mean detection rates across five seeds.

| fault_type | 0 | 0.01 | 0.05 | 0.1 | 0.2 | 0.4 |
| --- | --- | --- | --- | --- | --- | --- |
| drift | 0 | 0 | 0 | 0 | 1 | 1 |
| duplicates | 0 | 0 | 1 | 1 | 1 | 1 |
| fairness | 0 | 0 | 0 | 0.8 | 1 | 1 |
| missingness | 0 | 0 | 0 | 0 | 1 | 1 |
| pii | 0 | 0.6 | 1 | 1 | 1 | 1 |
| split_leakage | 0 | 1 | 1 | 1 | 1 | 1 |

First majority-detected nonzero severity:

| Fault Type | First Majority-Detected Severity | Clean Detection Rate |
| --- | --- | --- |
| drift | `0.20` | 0.00 |
| duplicates | `0.05` | 0.00 |
| fairness | `0.10` | 0.00 |
| missingness | `0.20` | 0.00 |
| pii | `0.01` | 0.00 |
| split_leakage | `0.01` | 0.00 |

Interpretation:

- Clean controls had zero primary detection rate.
- Critical categorical failures, especially leakage and PII, are detected at
  low severity.
- Continuous degradation modes are detected once configured warning thresholds
  are crossed.

## Policy Gate Failure Rate Matrix

Values are mean policy-failure rates across five seeds.

| fault_type | 0 | 0.01 | 0.05 | 0.1 | 0.2 | 0.4 |
| --- | --- | --- | --- | --- | --- | --- |
| drift | 0 | 0 | 0 | 0 | 1 | 1 |
| duplicates | 0 | 0 | 1 | 1 | 1 | 1 |
| fairness | 0 | 0 | 0 | 0.8 | 1 | 1 |
| missingness | 0 | 0 | 0 | 0 | 1 | 1 |
| pii | 0 | 0.6 | 1 | 1 | 1 | 1 |
| split_leakage | 0 | 1 | 1 | 1 | 1 | 1 |

Important improvement now implemented:

- Earlier, fairness-only risk was detected by the analyzer but passed the policy
  gate.
- `audit_history.DEFAULT_POLICY` now includes `max_positive_rate_disparity`.
- The policy gate now fails fairness runs whenever the fairness detection rule
  is triggered.

## Recommendation Coverage Rate Matrix

Values are mean recommendation-hit rates across five seeds.

| fault_type | 0 | 0.01 | 0.05 | 0.1 | 0.2 | 0.4 |
| --- | --- | --- | --- | --- | --- | --- |
| drift | 0 | 0 | 0 | 0 | 1 | 1 |
| duplicates | 0 | 0 | 1 | 1 | 1 | 1 |
| fairness | 0 | 0 | 0 | 0.8 | 1 | 1 |
| missingness | 0 | 0 | 0 | 0 | 1 | 1 |
| pii | 0 | 0.6 | 1 | 1 | 1 | 1 |
| split_leakage | 0 | 1 | 1 | 1 | 1 | 1 |

Detection, recommendation, and policy summary for nonzero severities:

| fault_type | detected_rate_mean | recommendation_rate_mean | policy_fail_rate_mean | mean_score | min_score | max_score |
| --- | --- | --- | --- | --- | --- | --- |
| drift | 0.4 | 0.4 | 0.4 | 84.36 | 78.2 | 91.4 |
| duplicates | 0.8 | 0.8 | 0.8 | 83.04 | 79.2 | 90.4 |
| fairness | 0.56 | 0.56 | 0.56 | 86.84 | 81.8 | 91.6 |
| missingness | 0.4 | 0.4 | 0.4 | 88.2 | 83.2 | 91.2 |
| pii | 0.92 | 0.92 | 0.92 | 68.4 | 66.4 | 76.4 |
| split_leakage | 1 | 1 | 1 | 79.4 | 79 | 79.6 |

Interpretation:

- Recommendations align with detected failures.
- Policy failures now align with detected fairness risk.
- Recommendation coverage currently follows thresholded detection. The paper
  should distinguish "not detected because below threshold" from false
  negatives.

## Generated Figures

The experiment runner automatically generated these paper-support figures:

| Figure | Path |
| --- | --- |
| Health score vs. severity | `experiments/results/tabular_fault_injection_5seed/figures/score_vs_severity.svg` |
| Primary metric vs. severity | `experiments/results/tabular_fault_injection_5seed/figures/primary_metric_vs_severity.svg` |
| Detection heatmap | `experiments/results/tabular_fault_injection_5seed/figures/detection_heatmap.svg` |
| Recommendation coverage heatmap | `experiments/results/tabular_fault_injection_5seed/figures/recommendation_heatmap.svg` |
| Policy-gate heatmap | `experiments/results/tabular_fault_injection_5seed/figures/policy_gate_heatmap.svg` |

The local runtime did not include `matplotlib`, so the runner used its
dependency-free SVG fallback. In an environment with `matplotlib`, the same
runner can emit PNG figures.

## Paper-Ready Claims Supported by This Run

Conservative claims supported:

1. ASTRID detects injected tabular PII and split leakage at low severity without
   false positives on the clean controls.
2. ASTRID's primary metrics are monotonic or near-monotonic with injected
   severity across six controlled tabular fault families.
3. ASTRID's global health score decreases under all injected reliability
   failures.
4. ASTRID recommendations are triggered when the corresponding fault crosses the
   active detection threshold.
5. ASTRID's policy gate now captures fairness disparity, not only quality,
   leakage, drift, and PII risks.
6. Runtime is low enough for initial CI/MLOps use on this synthetic tabular
   benchmark.

Claims not yet supported:

1. ASTRID outperforms baseline tools.
2. ASTRID generalizes to real industrial datasets.
3. ASTRID score degradation correlates with downstream model degradation.
4. ASTRID's thresholds are empirically optimal.
5. ASTRID detects time-series and image faults with the same behavior.

## Threats to Validity

Internal validity:

- The synthetic dataset is intentionally controlled, so results may be easier
  than messy real industrial data.
- Faults are injected independently; real datasets often contain interacting
  failures.
- Detection labels use ASTRID-aligned thresholds. A baseline-independent ground
  truth scoring protocol should be added.

External validity:

- This run covers only tabular data.
- The dataset is synthetic and does not capture domain-specific units, sensors,
  seasonality, or operational constraints.
- PII is represented by email-like strings only.

Construct validity:

- ASTRID's health score is a decision-support index, not a proof of dataset
  safety.
- Fairness is represented by group positive-rate disparity. Other fairness
  concepts are not tested here.
- PII severity is summarized as flagged-column count, which saturates early.

Conclusion validity:

- Repeated seeds are now included, but confidence intervals should still be
  reported explicitly in the paper.
- No baseline comparison is included yet.
- No downstream model degradation experiment is included yet.

## Required Next Experiments for a Top-Tier Paper

1. Add real public datasets from OpenML/UCI-style benchmarks.
2. Add baseline comparisons by fault category.
3. Add downstream model degradation experiments.
4. Add time-series fault injection.
5. Add image fault injection.
6. Add score sensitivity analysis over thresholds and weights.
7. Add runtime scaling over rows, columns, and modality.
8. Freeze experiment configuration in machine-readable YAML.
9. Archive code, configs, raw outputs, aggregate outputs, and figures.

## Immediate Engineering Follow-Ups

1. Export PII hit rate alongside flagged-column count in the summary CSV.
2. Generate LaTeX tables automatically from the aggregate CSV.
3. Add a baseline runner interface for category-specific comparisons.
4. Add downstream model degradation scripts.
5. Add a top-level CLI command for experiments.

## Suggested Paper Framing

Strongest framing:

> We introduce ASTRID, a local-first framework for pre-training dataset
> reliability assessment, and evaluate it through controlled multimodal
> fault-injection benchmarks that quantify detection behavior, score
> sensitivity, recommendation coverage, and governance-gate outcomes.

For a top-tier submission, avoid framing this as a Streamlit tool paper. The
scientific contribution should be the taxonomy, operationalized measurements,
controlled benchmark protocol, and empirical validation.
